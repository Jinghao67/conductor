interface:
  display_name: "Conductor"
  short_description: "Named sessions, clean master, sidecar."
  default_prompt: "Use $conductor to keep this as the clean master session, create named branch cards before opening sessions, use CD-DISPATCH for routing debate, use CD-E01 as a context-rich dirty explainer, and merge only approved completion reports."

policy:
  allow_implicit_invocation: true
