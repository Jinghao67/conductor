interface:
  display_name: "Conductor"
  short_description: "Clean master, ordered branches, sidecar."
  default_prompt: "Use $conductor to split this complex task into dependency-aware interactive branches, keep the master session clean, and only merge approved completion reports."

policy:
  allow_implicit_invocation: true
