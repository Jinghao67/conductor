interface:
  display_name: "Trunkline"
  short_description: "Clean trunk, dirty sidecar, live branches."
  default_prompt: "Use $trunkline to split this complex task into interactive branches, keep the master session clean, and only merge approved completion reports."

policy:
  allow_implicit_invocation: true
