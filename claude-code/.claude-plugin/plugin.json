{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "conductor-cc",
  "version": "0.1.0",
  "description": "Context-hygiene and branch-orchestration for long-running work. Keeps the master session clean; quarantines raw content in separate branch sessions and read-firewall subagents; enforces the protocol with hooks instead of relying on the model to comply.",
  "author": {
    "name": "Conductor"
  },
  "keywords": ["context", "orchestration", "branches", "hooks", "sessions", "hygiene"],
  "hooks": "./hooks/hooks.json",
  "skills": "./skills/",
  "commands": "./commands/",
  "agents": ["./agents/cd-merge-verify.md"]
}
